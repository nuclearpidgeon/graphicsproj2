<Page
    x:Name="pageRoot"
    x:Class="Project2.OptionsPage"
    DataContext="{Binding DefaultViewModel, RelativeSource={RelativeSource Self}}"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Project2"
    xmlns:common="using:Project2.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <!-- Collection of items displayed by this page -->
        <CollectionViewSource
        x:Name="itemsViewSource"
        Source="{Binding Items}"/>
    </Page.Resources>

    <!--
        This grid acts as a root panel for the page that defines two rows:
        * Row 0 contains the back button and page title
        * Row 1 contains the rest of the page layout
    -->
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="140"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Back button and page title -->
        <Grid x:Name="titlePanel">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="backButton" Margin="39,59,39,0" Command="{Binding NavigationHelper.GoBackCommand, ElementName=pageRoot}"
                        Style="{StaticResource NavigationBackButtonNormalStyle}"
                        VerticalAlignment="Top"
                        AutomationProperties.Name="Back"
                        AutomationProperties.AutomationId="BackButton"
                        AutomationProperties.ItemType="Navigation Button"/>
            <TextBlock x:Name="pageTitle" Text="Options" Style="{StaticResource HeaderTextBlockStyle}" Grid.Column="1" 
                        IsHitTestVisible="false" TextWrapping="NoWrap" VerticalAlignment="Bottom" Margin="0,0,0,40"/>
        </Grid>

        <Canvas HorizontalAlignment="Center" Height="Auto" Grid.Row="2" Width="500"  VerticalAlignment="Top" Margin="0,50,0,0">
            <Grid HorizontalAlignment="Left" Grid.Row="1" VerticalAlignment="Top" Width="500" Height="400" >
                <Grid.RowDefinitions>
                    <RowDefinition Height="60"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="60"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="60"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <!-- Row 0 & 1 -->
                <TextBlock Text="Lighting" FontSize="24" FontWeight="Bold" Grid.ColumnSpan="3" TextAlignment="Center" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                <Button x:Name="lighting_low_btn" FontSize="24" Content="Low" Grid.Row="1" Grid.Column="0" HorizontalAlignment="Center" Click="LightingLowClick"/>
                <Button x:Name="lighting_med_btn" FontSize="24" Content="Medium" Grid.Row="1" Grid.Column="1" HorizontalAlignment="Center" Click="LightingMediumClick"/>
                <Button x:Name="lighting_high_btn" FontSize="24" Content="High" Grid.Row="1" Grid.Column="2" HorizontalAlignment="Center" Click="LightingHighClick"/>
                <!-- Row 2 & 3 -->
                <TextBlock Text="Textures" FontSize="24" FontWeight="Bold" Grid.Row="2" Grid.ColumnSpan="3" TextAlignment="Center" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                <Button x:Name="txture_low_btn" FontSize="24" Content="Low" Grid.Row="3" Grid.Column="0" HorizontalAlignment="Center" Click="TextureLowClick" />
                <Button x:Name="txture_med_btn" FontSize="24" Content="Medium" Grid.Row="3" Grid.Column="1" HorizontalAlignment="Center" Click="TextureMediumClick"/>
                <Button x:Name="txture_high_btn" FontSize="24" Content="High" Grid.Row="3" Grid.Column="2" HorizontalAlignment="Center" Click="TextureHighClick"/>
                <!-- Row 4 & 5 -->
                <TextBlock Text="Accelerometer Sensitivity" FontSize="24"  FontWeight="Bold" Grid.Row="4" Grid.ColumnSpan="3" TextAlignment="Center" HorizontalAlignment="Center" Margin="0,20,0,0" VerticalAlignment="Center"/>
                <Slider x:Name="accelSensitiveSlider" HorizontalAlignment="Left" Grid.Row="5" VerticalAlignment="Top" Grid.ColumnSpan="3" Width="500" Maximum="2" Minimum="0.2" LargeChange="0.1" SmallChange="0.05" StepFrequency="0.05" TickFrequency="3"/>
            </Grid>
        </Canvas>
    </Grid>
</Page>
